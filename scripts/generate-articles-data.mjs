// Placeholder script for generating articles data
// Currently articles are managed via content/*.md files

export default function generateArticlesData() {
  console.log('Articles data generation skipped (using markdown files directly)');
}

generateArticlesData();
